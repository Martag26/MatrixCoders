{
  "statusLine": {
    "type": "command",
    "command": "input=$(cat); printf 'matrixcoders | %s | %s' \"$(echo \"$input\" | jq -r '.model.display_name')\" \"$(echo \"$input\" | jq -r '.workspace.current_dir')\""
  }
}
